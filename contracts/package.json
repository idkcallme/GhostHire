{
  "name": "@ghosthire/contracts",
  "version": "1.0.0",
  "description": "GhostHire Compact contracts",
  "private": true,
  "scripts": {
    "build": "echo 'Mock build - contracts would be compiled here'",
    "test": "echo 'Mock test - contract tests would run here'",
    "deploy": "echo 'Mock deploy - contracts would be deployed here'",
    "deploy:local": "echo 'Mock local deploy - contracts would be deployed locally here'",
    "clean": "rm -rf dist build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist/**/*",
    "build/**/*"
  ]
}
