{
  "name": "@ghosthire/contracts",
  "version": "1.0.0",
  "description": "GhostHire Midnight Network Smart Contracts",
  "private": true,
  "scripts": {
    "build": "compact compile JobBoard.compact --output ./build",
    "compile": "compact compile JobBoard.compact --output ./build",
    "deploy": "node scripts/deploy.js",
    "deploy:testnet": "cross-env NETWORK=testnet node scripts/deploy.js",
    "deploy:local": "cross-env NETWORK=local node scripts/deploy.js",
    "test": "jest",
    "clean": "rm -rf dist build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0"
  },
  "files": [
    "dist/**/*",
    "build/**/*"
  ]
}
