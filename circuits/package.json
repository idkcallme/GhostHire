{
  "name": "@ghosthire/circuits",
  "version": "1.0.0",
  "description": "Zero-Knowledge circuits for GhostHire eligibility verification",
  "scripts": {
    "compile": "circom eligibility.circom --r1cs --wasm --sym --c",
    "setup": "snarkjs groth16 setup eligibility.r1cs powersOfTau28_hez_final_15.ptau circuit_0000.zkey",
    "generate-vkey": "snarkjs zkey export verificationkey circuit_0000.zkey verification_key.json",
    "clean": "rm -rf *.r1cs *.sym *.wasm *.zkey *.json eligibility_js/",
    "test": "node test-circuit.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.3"
  },
  "devDependencies": {
    "circom": "^2.1.6"
  },
  "keywords": ["zero-knowledge", "circom", "privacy", "eligibility-proof"],
  "author": "GhostHire Team",
  "license": "Apache-2.0"
}
